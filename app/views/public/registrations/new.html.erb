
  <p id="notice"></p>
  <div class="container px-sm-0">
    <div id="error_explanation">
    </div>
    <div class="row ">
      <div class="col-md-3 bg-light text-center"><h3>新規会員登録</h3></div>
    </div>
    <div class="row ">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "public/shared/error_messages", resource: resource %>
        <div class="col-md-12 my-4 mx-auto">
          <table class="table table-borderless">
            <tr>
              <th><%= f.label :名前, class: "px-5" %></th>
              <th>（姓）　<%= f.text_field :last_name %>　　（名）　<%= f.text_field :first_name %></th>
            </tr>
            <tr>
              <th><%= f.label :フリガナ, class: "px-5" %></th>
              <th>（セイ）<%= f.text_field :last_name_kana %>　　（メイ）<%= f.text_field :first_name_kana %></th>
            </tr>
            <tr>
              <th><%= f.label :郵便番号, class: "px-5" %></th>
              <th><%= f.text_field :postal_code %></th>
            </tr>
            <tr>
              <th><%= f.label :住所, class: "px-5" %></th>
              <th><%= f.text_field :address, size: "60" %></th>
            </tr>
            <tr>
              <th><%= f.label :電話番号, class: "px-5" %></th>
              <th><%= f.text_field :telephone_number %></th>
            </tr>
            <tr>
              <th><%= f.label :メールアドレス, class: "px-5" %></th>
              <th><%= f.text_field :email %></th>
            </tr>
            <tr>
              <th><%= f.label :パスワード（６文字以上） %></th>
              <th><%= f.password_field :password, autocomplete: "new-password" %></th>
            </tr>
            <tr>
              <th>パスワード（確認用）</th>
              <th><%= f.password_field :password_confirmation, autocomplete: "new-password" %></th>
            </tr>
            <tr>
              <th></th>
              <th><%= f.submit "新規登録", class: "btn btn-success btn-lg w-50" %></th>
            </tr>
          </table>
        </div>
      <% end %>
    </div>
    <div class="row ">
      <div class="col-md-3 bg-light text-center"><h3>既に登録済みの方</h3></div> 
    </div>
    <table class="ml-5 mt-3 ">
      <tfoot>
        <tr>
          <td style="font-size: 20px;"><%= render "public/shared/links" %>　からログインしてください。</td>
        </tr>
      </tfoot>
    </table>
  </div>
